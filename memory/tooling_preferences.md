# Tooling Preferences

## Codex configuration

- On 2026-08-13, the project-local Codex config at `.devcontainer/codex-config.toml` was updated to use `model = "gpt-5.5"` and `model_reasoning_effort = "high"`.
- Note: `sol` is a GPT-5.6 family variant, not a GPT-5.5 variant. The applied configuration reflects the valid GPT-5.5 equivalent of the user's requested capability level.
