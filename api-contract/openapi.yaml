# <projectname> API Contract
# Status: PLACEHOLDER — will be defined after PRD + interface design session
#
# This file is the single source of truth between frontend (Claude) and backend (Codex).
# Changes require a proposal in docs/api-proposals/ and agreement from both sides.
#
# To mock locally (frontend dev):
#   npx @stoplight/prism-cli mock api-contract/openapi.yaml --port 4010

openapi: "3.1.0"
info:
  title: <projectname> API
  version: "0.0.1-draft"
  description: |
    TBD

servers:
  - url: http://localhost:4010
    description: Prism mock server (frontend dev)
  - url: http://localhost:5000
    description: Local backend (Codex dev)

paths: {}
# Paths will be defined in the interface design session (next session)

components:
  schemas: {}
  # Schemas will be derived from the canonical domain model after PRD
